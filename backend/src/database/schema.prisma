generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("PLATFORM_DB_URL")
}

model company {
  id Int @id @default(autoincrement())
}

model user {
  id Int @id @default(autoincrement())
}
